/* Basic Reset */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

/* Header Styling */
header {
  background-color: #333;
  color: #fff;
  padding: 1rem 0;
  text-align: center;
}

/* Responsive Design */
@media (max-width: 768px) {
  .product-card {
    width: 100%;
    margin: 1rem 0;
  }
}
